export const content = `
许多网站、应用程序和邮件平台都有严格的文件大小限制。头像必须在 100KB 以下，表单附件不能超过 200KB，而你的照片有 3MB，完全不知道该怎么办。

本指南将向你介绍将图片压缩至 200KB 以内的最简便方法——无需安装任何软件，也不必将照片上传到陌生的服务器。

## 为什么文件需要控制在 200KB 以内？

文件大小限制的存在是出于带宽、存储和加载速度方面的考虑。以下是常见的场景：

- **政府和签证申请表** — 许多要求照片在 200KB 甚至 50KB 以下
- **求职平台** — 简历照片上传通常有严格的大小限制
- **邮件附件** — 部分企业邮件系统会屏蔽过大的附件
- **WhatsApp 等即时通讯应用** — 图片会自动压缩，但有时需要指定大小
- **网站上传** — 头像、产品图片、用户图标

令人沮丧的是，现代手机摄像头拍出的图片通常在 3MB 到 10MB 之间。一张 iPhone 照片可能高达 8MB——是 200KB 限制的 40 倍。

## 方法一：使用 NanoImage（免费，无需上传）

将图片压缩至 200KB 以内最快的方式，是使用 [NanoImage 压缩工具](/compress-image)。

**NanoImage 的不同之处：** 大多数在线压缩工具会先将你的图片上传至服务器，处理后再发回。你的照片会经过一家你完全不了解的公司的计算机。NanoImage 则直接在你的浏览器中处理一切——图片始终不离开你的设备。

**操作步骤：**

1. 访问 [nanoimage.net/compress-image](/compress-image)
2. 点击 **上传图片** 或将照片拖入界面
3. 工具自动找到最优压缩方案，将文件压至 200KB 以下
4. 点击 **下载** 保存压缩后的图片

就这么简单。无需账号，不添加水印，完全免费。

**支持哪些文件类型？** JPG、PNG、WebP 和 GIF。

## 方法二：手动调整大小

如果你需要的目标大小不是 200KB，可以在大多数压缩工具中手动调整质量滑块。

几点注意事项：

- **JPEG 压缩** 通过减少人眼不敏感区域的细节来实现压缩。在 80% 质量下，大多数人看不出与原图的差别。在 60% 质量下，细节丰富的区域可能开始出现模糊。
- **PNG 文件** 默认是无损的。在压缩前将 PNG 转换为 JPG，通常能得到小得多的文件。
- **分辨率同样重要。** 4000×3000 像素的图片在相同质量下始终比 1200×900 的图片更大。如果图片只用于屏幕显示而非打印，先缩小尺寸会非常有效。

## 方法三：先缩小尺寸，再压缩

有时候，最有效的方法是在压缩之前先调整图片尺寸。

对于大多数屏幕用途，图片宽度不需要超过 1200 像素。如果原图有 4000 像素宽，将其调整到 1200 像素，在尚未应用任何压缩的情况下，文件大小就能减少约 90%。

你可以在 NanoImage 中分两步完成：

1. 先使用 [调整大小工具](/resize-image) 缩小尺寸
2. 再使用 [压缩工具](/compress-image) 达到目标文件大小

## 压缩后图片质量会怎样？

这是最常见的顾虑。简短的回答是：**在日常使用中，你不会注意到任何差别。**

实际发生的变化：

- 非常细微的细节（如织物纹理或树上的叶片）会略微变得平滑
- 纯色区域（如蓝天或白色背景）几乎不受影响
- 对于人像照片，肤色和面部在相当高的压缩比下通常依然好看

一张从 5MB 压缩至 200KB 的照片，在手机屏幕或网页上看起来几乎一模一样。只有在非常近距离放大或大尺寸打印时，才可能看出明显差异。

## 获得最小文件大小的技巧

- **照片使用 JPG 而非 PNG。** PNG 最适合含文字或透明背景的图形。对于照片，JPG 在相近质量下几乎总是更小。
- **移除元数据（EXIF 数据）。** 相机会将 GPS 位置、设备信息和拍摄参数嵌入每张照片。删除这些数据可以为部分图片节省 10-50KB。
- **先缩小尺寸，再压缩。** 如果图片将在屏幕上以 400×400 像素显示，就没必要保持 3000×3000 像素。

## 常见问题

**压缩到 200KB 会损坏我的原始照片吗？**
不会。NanoImage 下载的是一个新的压缩副本，你设备上的原始文件不受任何影响。

**最大压缩后图片仍然太大怎么办？**
如果图片在最低质量下仍超过 200KB，说明图片的像素尺寸太大。请先使用 [调整大小工具](/resize-image) 缩小像素尺寸，然后再次压缩。

**在线压缩敏感照片安全吗？**
使用 NanoImage 是安全的——因为你的图片从不离开你的浏览器。而使用其他上传至服务器的工具，则需要信任该公司对你图片数据的处理方式。在上传敏感文件之前，请务必查阅工具的隐私政策。

**可以一次压缩多张图片吗？**
批量压缩功能已在 NanoImage 的开发计划中。目前，图片一次处理一张。

## 总结

一旦找到正确的工具，将图片压缩至 200KB 其实非常简单。关键要点：

- 使用 NanoImage 这类基于浏览器的工具，让图片保持私密
- 对于大尺寸照片，先缩小尺寸再压缩
- JPG 格式能为照片提供最小的文件大小
- 200KB 的质量完全能满足网页、邮件和大多数表单上传的需求
`;
