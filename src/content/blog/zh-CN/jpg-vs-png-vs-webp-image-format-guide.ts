export const content = `
# JPG vs PNG vs WebP——应该用哪种图片格式？

如果你曾经想知道为什么有这么多图片格式，以及选哪种格式是否重要——这篇指南就是为你写的。

简短的答案：不同格式在文件大小、质量和透明度等功能上做出了不同的权衡。选错格式可能意味着不必要的大文件、明显的质量损失，或兼容性问题。

以下是你需要了解的一切，用简单易懂的语言呈现。

---

## 三种格式一览

| | JPG | PNG | WebP |
|---|---|---|---|
| **压缩** | 有损 | 无损 | 两者皆支持（有损和无损） |
| **透明度** | ❌ 不支持 | ✅ 支持 | ✅ 支持 |
| **文件大小** | 小 | 中到大 | 最小 |
| **最适合** | 照片 | 图形、Logo、截图 | 网页图片（所有类型） |
| **浏览器支持** | 通用 | 通用 | 现代浏览器（95%+） |
| **编辑质量** | 每次保存都会降低 | 无质量损失 | 损失极小（无损模式） |

---

## JPG（JPEG）——照片的标准格式

JPG 自 1990 年代起就是照片的主流格式。它通过分析图片并丢弃人眼最不可能注意到的视觉信息来工作——这个过程称为**有损压缩**。

### JPG 压缩的工作原理
保存 JPG 时，编码器将图片分成 8×8 像素的块，并对每个块应用数学变换（离散余弦变换）。高频细节（锐利边缘、精细纹理）比低频信息（平滑渐变、大面积相似颜色区域）压缩得更激进。

在高质量设置（85-100%）下，这个过程几乎不可见。在低质量设置（60% 以下）时，你开始看到特有的"瑕疵"——尤其是边缘和文字周围出现块状斑点。

### 何时使用 JPG
- ✅ 照片和真实场景图像
- ✅ 电商产品照片
- ✅ 需要通过邮件分享的图片（文件小）
- ✅ 任何文件大小比像素级完美质量更重要的场合

### 何时不该使用 JPG
- ❌ 含文字的图片（压缩瑕疵使文字看起来模糊）
- ❌ Logo 和图标（锐利边缘会变得块状）
- ❌ 有透明背景的图片
- ❌ 会反复编辑和保存的文件（每次保存质量都会下降）

---

## PNG——质量和透明度的冠军

PNG 使用**无损压缩**——不会丢弃任何视觉信息。你输入什么，输出就是什么，每个像素都一模一样。这使 PNG 成为精确度重要的图片的理想选择：Logo、截图、图形，以及任何需要透明度的内容。

### 透明度优势
PNG 支持 **alpha 通道**——一个为每个像素存储透明度信息的第四数据层。这意味着图片的某些部分可以完全透明、完全不透明，或介于两者之间。这对以下内容至关重要：
- 放置在不同颜色背景上的 Logo
- UI 元素（按钮、图标）
- 贴纸和叠加层

JPG 根本无法做到这一点。如果你试图将透明图片保存为 JPG，透明区域将填充为纯色（通常是白色）。

### PNG 文件大小
无损质量的代价是更大的文件。一张 PNG 照片可能比等效 JPG 大 3-5 倍。对于网页用途，这对照片来说是一个明显的缺点——但对于 Logo 和图形来说完全可以接受，因为这类图片视觉元素更简单，压缩效率更高。

### 何时使用 PNG
- ✅ Logo 和品牌素材
- ✅ 截图（尤其是含 UI 元素和文字的）
- ✅ 需要透明度的图片
- ✅ 含纯色、锐利线条或文字的图形
- ✅ 会反复编辑的源文件

### 何时不该使用 PNG
- ❌ 网页上的照片（太大，请改用 JPG 或 WebP）
- ❌ 文件大小是首要考虑时

---

## WebP——现代网络格式

WebP 由 Google 开发，于 2010 年发布。它被设计为两全其美：文件大小比 JPG 和 PNG 都小，同时支持透明度，并提供有损和无损两种模式。

### 大小优势
WebP 通常比同等质量的 JPG 小 **25-35%**，比同等 PNG 小 **26%**。

对于网站来说，这是显著的带宽节省——图片加载速度直接影响用户体验和搜索引擎排名。

### 兼容性
WebP 现在被所有主流现代浏览器支持：Chrome、Firefox、Safari（自 2020 年起）、Edge 和 Opera。截至 2024 年，全球浏览器支持率超过 95%。但一些较旧的软件和平台不支持它——尤其是较旧的邮件客户端和某些图像编辑器。

### 何时使用 WebP
- ✅ 对性能有要求的网页图片
- ✅ 想要在最小质量损失下实现最大压缩
- ✅ 需要快速加载的透明图片
- ✅ 任何 JPG 或 PNG 都能工作、但想要更小文件的场景

### 何时不该使用 WebP
- ❌ 目标平台对 WebP 支持有限（如较旧的邮件客户端）
- ❌ 需要与旧版软件兼容时
- ❌ 在不支持 WebP 的应用中编辑时

---

## 实际文件大小对比

以下是一张典型的 2000×1500px 照片的示例：

| 格式 | 设置 | 文件大小 |
|---|---|---|
| PNG（无损） | — | ~8.5 MB |
| JPG | 90% 质量 | ~1.8 MB |
| JPG | 80% 质量 | ~1.1 MB |
| WebP | 80% 质量 | ~780 KB |
| WebP | 无损 | ~6.2 MB |

对于照片，80% 质量的 WebP 能提供最佳平衡——视觉质量与 80% 的 JPG 相近，但体积约小 30%。

---

## GIF 和 BMP——简要说明

你有时会遇到另外两种格式：

**GIF** — 支持动画（因此在表情包和反应图中广受欢迎）。仅限 256 色，不适合照片。对于静态图片，PNG 始终更好。对于动画，可考虑 WebP（支持动画）或视频格式。

**BMP** — 未压缩的 Windows 格式。文件体积巨大（一张 1920×1080 的 BMP 通常超过 6 MB）。除了遗留的 Windows 应用程序，几乎没有理由使用 BMP。

---

## 快速决策指南

**是照片吗？** → 使用 **JPG**（如果平台支持，也可以用 WebP）

**需要透明背景吗？** → 使用 **PNG**（或 WebP）

**是 Logo、图标或截图吗？** → 使用 **PNG**

**要放到网站上，想要尽可能小的文件？** → 使用 **WebP**

**之后还会继续编辑吗？** → 使用 **PNG**（避免重复保存导致质量损失）

**需要在所有邮件客户端和旧版软件中正常显示？** → 使用 **JPG** 或 **PNG**

---

## 格式之间的转换

需要将图片从一种格式转换为另一种？[NanoImage 的 JPG 转换工具](https://nanoimage.net/convert-to-jpg) 可以在你的浏览器中即时处理 PNG、WebP、GIF 和 BMP → JPG 的转换。

---

## 相关工具

- **[压缩图片](https://nanoimage.net/compress-image)** — 在不切换格式的情况下减小 JPG 或 PNG 文件大小
- **[转换为 JPG](https://nanoimage.net/convert-to-jpg)** — 将 PNG、WebP、GIF、BMP 转换为 JPG
- **[调整图片大小](https://nanoimage.net/resize-image)** — 改变图片尺寸
`;
